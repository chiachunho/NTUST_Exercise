#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>


int main() {

	char train_data[100][100] = {0};
	int default;
	char in[100];
	char default_str[100];
	while (gets(in) != NULL) {
		if (strcmp(in, "Training data :")) {
			gets(default_str);

		}

	}
	return 0;
}
